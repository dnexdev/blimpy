#pragma once
// Copy this file to secrets.h (git-ignored) and fill in the LAPTOP HOTSPOT credentials.
// Never use the hackathon WiFi: it blocks device-to-device UDP.
#define WIFI_SSID "wisp"
#define WIFI_PASS "wispwisp"
